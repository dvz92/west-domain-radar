# 定向核查清单：每行一个域名，# 开头为注释。
# 每次运行会用 domkey 批量问一次接口（deldate=wei 覆盖所有待删除日期），
# 报告第五节列出"是否还在可预订状态"、真实删除日期，以及**本轮排名**。
#
# ⚠️ 只有"真实删除日期 == 该后缀本批次日期"的域名才会并进候选池参与排序；
#    不属于本批次的只在第五节列出并标注 📌（2026-09-26 修）。
#    这样即使池子太大只能抽样，你点名的域名也不会被漏掉，但也不会凭空混进榜单。
kombio.com
sery.cn
glax.cn
atiron.com
korp.cn

# 2026-09-26 用户点名核对批次用（都已确认在对应批次里）
coion.com
hecy.cn
loho.top
swoosh.top
